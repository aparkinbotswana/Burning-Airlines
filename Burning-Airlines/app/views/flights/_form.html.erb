<%= form_for(flight) do |f| %>
  <% if flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

      <ul>
      <% flight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.number_field :name %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :to %>
    <%= f.text_field :to %>
  </div>

  <div class="field">
    <%= f.label :from %>
    <%= f.text_field :from %>
  </div>





  <!-- <div class="field">
    <%= f.label :airplane_id %>
    <%= f.number_field :airplane_id %>
  </div> -->

  <!--  -->

  <div class="field">
    <%= f.label :airplane %>
    <%= collection_select(  :flight, :airplane_id, Airplane.all, :id, :name, {}, { :multiple => false } ) %>
  </div>

  <div class="field">
    <%= f.label :airplane %>
    <%= collection_select(  :flight, :airplane_id, Airplane.all, :id, :name, {}, { :multiple => false } ) %>
  </div>




  <!--  -->







  <!-- <%= label_tag :plane %>
  <%= text_field_tag :plane %>
  <br><br> -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
